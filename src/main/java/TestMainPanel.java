import javax.swing.*;
import java.awt.*;
import java.util.Arrays;
import java.util.List;

/**
 * Created by Briareus on 19.05.2015.
 */
public class TestMainPanel extends JPanel
{

   /* List<Integer> values = Arrays.asList(20, 33, 60, 10);
    TestDrawingPanel drawingPanel = new TestDrawingPanel(one);
    setLayout(new BorderLayout());
    add(drawingPanel);*/
}
